{
  "name": "@tokenring-ai/mcp",
  "version": "0.2.0",
  "description": "Client for connecting agents to external tools and resources via the MCP standard.",
  "dependencies": {
    "@tokenring-ai/app": "0.2.0",
    "@tokenring-ai/chat": "0.2.0",
    "@tokenring-ai/agent": "0.2.0",
    "@ai-sdk/mcp": "^1.0.35",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "ai": "^6.0.149",
    "zod": "^4.3.6"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  },
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "vitest": "^4.1.1",
    "typescript": "^6.0.2"
  },
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  }
}
